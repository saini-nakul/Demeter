
def count_sentiment(polarity_score_dict):

    count_pos = 0
    count_neg = 0
    count_neu = 0
    
    print(polarity_score_dict)

    # for comment, score in polarity_score_dict.items:
    #     print(comment, score)

    #     if score["compound"] > 0.05:
    #         count_pos = count_pos + 1
    #     elif score["compound"] < 0.05 and score["compound"] > -0.05:
    #         count_neu = count_neu + 1
    #     else:
    #         count_neg = count_neg + 1

    
    # return count_pos, count_neu, count_neg

    